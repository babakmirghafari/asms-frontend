apiVersion: v1
kind: Service
metadata:
  name: {{ include "asms-frontend.fullname" . }}
  labels:
    {{- include "asms-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "asms-frontend.selectorLabels" . | nindent 4 }}
