apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "asms-frontend.fullname" . }}
  labels:
    {{- include "asms-frontend.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "asms-frontend.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "asms-frontend.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 80
              protocol: TCP
          env:
            - name: API_BASE_URL
              value: {{ .Values.env.API_BASE_URL | quote }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
